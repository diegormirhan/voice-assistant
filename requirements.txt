# --- Screenshot (C puro) ---
mss>=10.2.0
Pillow>=12.3.0

# --- Audio I/O (PortAudio, mantido ativamente) ---
sounddevice>=0.5.5
numpy>=2.5.1

# --- VAD: Silero VAD (ONNX via silero-vad, torch só pra I/O) ---
silero-vad>=6.0.0

# --- STT: sherpa-onnx (ONNX Runtime, sem torch) ---
sherpa-onnx>=1.13.4

# --- TTS: Piper (ONNX) ---
piper-tts==1.6.0

# --- LLM: cliente Ollama (server roda Vulkan/AMD) ---
ollama>=0.6.2
